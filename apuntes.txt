============================================================
FIX RESERVAS — EJECUTAR UNA SOLA VEZ (08/04/2026)
============================================================
1. Activar venv y aplicar fix del enum en BD:
   cd backend
   venv\Scripts\activate
   python fix_notif_enum.py

2. Confirmar que dice: "reserva_cancelada_por_cliente agregado"

3. Levantar backend normal:
   uvicorn app.main:app --reload --port 8000

BUGS CORREGIDOS:
- Cancelacion de reserva ya no falla (notificacion es no-critica)
- Error al cargar reservas: timeout aumentado a 30s
- UniqueConstraint: ya no bloquea reservas de slots cancelados
- Nuevo endpoint DELETE /admin/reservas/{id}
============================================================

PARA LEVANATR EL BACKEND DE PICHANGAYA

venv\Scripts\activate
uvicorn app.main:app --reload --port 8000

Lanza el emulador Pixel 7----
Abre una terminal CMD nueva (o usa la misma) y corre:
flutter emulators --launch Pixel_7

PARA LEVANATR EL FRONTEND DE PICHANGAYA

flutter run -d chrome --web-port 3000



Corre la app
Una vez el emulador esté listo, vuelve a la terminal del frontend y corre:
flutter run



claude --resume 293a38d8-0d91-4a95-8bbe-4e5669460d66


para levantar en ngrok

ngrok http 3000 --host-header="localhost:3000"
ngrok http 127.0.0.1:3000 --host-header="localhost:3000"




MEJORAS PARA EL ARTEFACTO EN HTML PICHANGAYA.

ya cumpa quiero que en el rol de cliente agrgues un tab de productos que al ingesar muestre categorias como bebidas,
cervezas, snaks, y que al abrir en uno de ellos muestres todos los productos en cada producto la catidad de stock,   
precio del producto y boton de agrgar la cantidad  y  al hacer clip se sume a un carrito de compras donde se vea la
cantidad de prodcutos el total a pagar y al hace clip en pagar le aparezca las opciones de pago yape, plin, efectivo 
y que al admin le avise que el cliente le hizo un pedido y le lleve a su mesa. 

////PARA ACTIVAR EN WEB EL PUSH NOTIFICACION EN FIREBASE 
npm install firebase


 // Import the functions you need from the SDKs you need
import { initializeApp } from "firebase/app";
// TODO: Add SDKs for Firebase products that you want to use
// https://firebase.google.com/docs/web/setup#available-libraries

// Your web app's Firebase configuration
const firebaseConfig = {
  apiKey: "AIzaSyBIBDKwugvWYH9s7A2KYZdq7lboyxNAK_k",
  authDomain: "pichangaya-6d300.firebaseapp.com",
  projectId: "pichangaya-6d300",
  storageBucket: "pichangaya-6d300.firebasestorage.app",
  messagingSenderId: "83990636031",
  appId: "1:83990636031:web:da3dec3a2653e9de1473ca"
};

// Initialize Firebase
const app = initializeApp(firebaseConfig);

 ///// EL VAPID KEY PARA EL PUSH WEB DE NOTIFICACIONES EN FIREBASE

BO-zL00Gw34QLvk5Zh45wQYdjqLFpusN0l2Z3mUX_to-wIUvhGYW6rY9kgEzWy3sdTKKz5zs8T8dR5gd8GxPrWI
